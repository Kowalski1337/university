#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie();
    ios_base::sync_with_stdio(0);

    int n;
    cin >> n;

    vector<int> ans[1000001];
    ans[1] = {1};

    for (int i = 0; i < 1000; i++){

    }
    return 0;
}